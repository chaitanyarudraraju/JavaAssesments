package order;

import java.util.Arrays;

public class Sorting {
    public static void main(String[] args) {
        int numbers[] = {4, 3, 5, 6, 1, 2};
        int length = numbers.length;
        boolean flag =false;

        Sort order=new Sort();
        order.sort(length,numbers,flag);


    }}

